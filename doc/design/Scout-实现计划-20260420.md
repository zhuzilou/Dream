# Scout (新闻收集员) 实现计划 (暂停留存)

## 状态
**当前状态：** 今日暂不开发，仅保存计划文档留待后续执行。

## 一、 目标 (Objective)
在完全隔离的 Docker 沙箱环境中，开发并部署符合 `投资团队搭建方案-20260419.md` 要求的“Scout（侦察员）”信息采集系统。该系统将定时抓取金融新闻和市场数据，并将其**输出为结构化的 Markdown 文件**，方便阅读和检索。

**核心约束：**
所有的代码、配置和输出文件**必须且仅限**存放在当前宿主机目录 `/Users/zhuzilou/workspace/ai/gemini/Dream/` 下。所有的运行依赖均在 Docker 容器内安装，确保宿主机系统“零污染”。

## 二、 核心文件与结构设计 (设计图)
```text
/Users/zhuzilou/workspace/ai/gemini/Dream/
├── docker-compose.yml          # Docker 服务编排文件
├── Dockerfile                  # Python 运行环境定义
├── requirements.txt            # Python 依赖 (akshare, pandas, schedule 等)
├── data/                       # 数据持久化目录 (映射至 Docker)
│   ├── reports/                # 生成的 Markdown 新闻简报目录
│   └── scout.db                # SQLite 数据库文件 (用于记录已抓取ID防重)
└── src/                        # 核心源代码目录
    ├── main.py                 # 主入口与定时调度器
    ├── scraper/
    │   └── akshare_client.py   # 基于 AkShare 的数据抓取逻辑
    └── generator/
        └── markdown_gen.py     # Markdown 报告生成逻辑
```

## 三、 未来执行步骤 (Future Execution Steps)

1. **环境配置**：编写 Dockerfile、requirements.txt 和 docker-compose.yml。
2. **防重机制**：使用 SQLite 记录已抓取新闻的 ID。
3. **抓取逻辑**：开发 akshare_client.py 接入财联社/东方财富等源。
4. **生成卡片**：开发 markdown_gen.py 将数据转为 Markdown 卡片输出到 data/reports。
5. **定时调度**：使用 schedule 库设定 08:30、12:30、18:30 自动执行。

## 四、 本次执行计划 (Today's Action)
1. 将本计划文档内容保存至工作区：`/Users/zhuzilou/workspace/ai/gemini/Dream/Scout-实现计划-20260420.md`。
2. 结束今日任务。
